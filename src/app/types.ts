export type Project = {
  title: string;
  description: string;
  tools: string[];
  link?: string;
  githubLink?: string;
};

export type Experience = {
  from: string;
  to: string;
  society: string;
  post: string;
  description: string;
};
